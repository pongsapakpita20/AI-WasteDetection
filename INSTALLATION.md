# 📦 คู่มือการติดตั้งระบบ AI Waste Sorter

คู่มือนี้จะแนะนำขั้นตอนการติดตั้งระบบคัดแยกขยะอัจฉริยะ (AI Waste Sorter) บน Windows

---

## 📋 ความต้องการของระบบ

### ซอฟต์แวร์ที่จำเป็น:
- **Python 3.11** หรือสูงกว่า (แนะนำ Python 3.11.9)
- **Git** (สำหรับ clone repository)
- **CUDA 13.0** หรือสูงกว่า (สำหรับ GPU acceleration - ไม่บังคับ)
- **Webcam** (สำหรับใช้งานระบบตรวจจับแบบ real-time)

### ฮาร์ดแวร์ที่แนะนำ:
- **GPU**: NVIDIA GPU ที่รองรับ CUDA (แนะนำ)
- **RAM**: อย่างน้อย 8 GB
- **Storage**: อย่างน้อย 5 GB สำหรับโมเดลและ dependencies

---

## 🚀 ขั้นตอนการติดตั้ง

### 1. Clone Repository

```bash
git clone <repository-url>
cd AI-Waste-Sorter
```

หรือถ้ามี repository อยู่แล้ว ให้เปิด terminal ในโฟลเดอร์โปรเจกต์

---

### 2. สร้าง Virtual Environment

#### สำหรับ Windows PowerShell:
```powershell
# สร้าง virtual environment ชื่อ .venv
python -m venv .venv

# เปิดใช้งาน virtual environment
& .\.venv\Scripts\Activate.ps1
```

#### สำหรับ Windows Command Prompt (CMD):
```cmd
# สร้าง virtual environment
python -m venv .venv

# เปิดใช้งาน virtual environment
.venv\Scripts\activate.bat
```

**หมายเหตุ:** ถ้า PowerShell แสดงข้อผิดพลาดเกี่ยวกับ execution policy ให้รันคำสั่งนี้ก่อน:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 3. อัปเดต pip และติดตั้ง Dependencies

```bash
# อัปเดต pip เป็นเวอร์ชันล่าสุด
python -m pip install --upgrade pip

# ติดตั้ง packages ทั้งหมดจาก requirements.txt
pip install -r requirements.txt
```

**หมายเหตุ:** การติดตั้งอาจใช้เวลานาน (10-30 นาที) ขึ้นอยู่กับความเร็วอินเทอร์เน็ตและเครื่องคอมพิวเตอร์
---

### 4. ดาวน์โหลด Dataset (ถ้ายังไม่มี)

ถ้ายังไม่มี dataset ให้ดาวน์โหลดผ่าน DVC:

```bash
# ดาวน์โหลด dataset จาก DVC storage
dvc pull waste-detection.dvc
```

หรือถ้าใช้ DVC remote storage อื่น ให้ตรวจสอบการตั้งค่าใน `.dvc/config`

---

### 5. ดาวน์โหลดโมเดล Pre-trained (ถ้ายังไม่มี)

ถ้ายังไม่มีโมเดลที่เทรนแล้ว (`artifacts/models/waste-sorter-best.pt`) ให้:

#### วิธีที่ 1: ดาวน์โหลดจาก DVC Storage
```bash
dvc pull artifacts/models/waste-sorter-best.pt.dvc
```

#### วิธีที่ 2: เทรนโมเดลใหม่
```bash
# เทรนโมเดล (ใช้เวลานาน)
python train.py

# หรือใช้ DVC pipeline
dvc repro train
```

ดูรายละเอียดเพิ่มเติมใน [README_PIPELINE.md](README_PIPELINE.md)

---

## ✅ ตรวจสอบการติดตั้ง

### ตรวจสอบ Python และ Packages:

```bash
# ตรวจสอบเวอร์ชัน Python
python --version

# ตรวจสอบว่า virtual environment เปิดใช้งานอยู่
python -c "import sys; print(sys.executable)"
# ควรแสดง path ที่มี .venv ในนั้น

# ตรวจสอบ packages สำคัญ
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import ultralytics; print('Ultralytics: OK')"
python -c "import gradio; print('Gradio: OK')"
python -c "import cv2; print(f'OpenCV: {cv2.__version__}')"
```

### ตรวจสอบ CUDA (ถ้ามี GPU):

```bash
python -c "import torch; print(f'CUDA Available: {torch.cuda.is_available()}')"
python -c "import torch; print(f'CUDA Version: {torch.version.cuda if torch.cuda.is_available() else \"N/A\"}')"
```

---

## 🎮 วิธีใช้งานระบบ

### 1. รันแอปพลิเคชันหลัก (Gradio Interface)

```bash
# ตรวจสอบว่า virtual environment เปิดใช้งานอยู่
# แล้วรันคำสั่ง:
python app.py
```

ระบบจะเปิดหน้าเว็บในเบราว์เซอร์อัตโนมัติ (ปกติที่ `http://127.0.0.1:7860`)

### 2. ใช้งานระบบ

1. เปิดหน้าเว็บที่แสดงขึ้นมา
2. คลิกที่กล้องเพื่อเปิดใช้งาน webcam
3. จ่อขยะที่กล้อง
4. ระบบจะตรวจจับและแสดงผลพร้อมคำแนะนำเสียง

---

## 🔧 การตั้งค่าเพิ่มเติม

### ปรับแต่งการตรวจจับ

แก้ไขในไฟล์ `app.py`:

```python
# Confidence threshold สำหรับการตรวจจับ
SPEECH_CONF_THRESHOLD = 0.4          # conf ขั้นต่ำที่จะพิจารณาพูด
SUSTAINED_FRAME_THRESHOLD = 2        # จำนวนเฟรมต่อเนื่องก่อนพูด
ANNOUNCE_COOLDOWN_SECONDS = 6        # เวลาระหว่างการพูดซ้ำคลาสเดิม
```

### ปรับแต่งการบันทึกภาพ

แก้ไขในไฟล์ `app.py`:

```python
SAVE_IMAGES = True                   # เปิด/ปิดการบันทึกภาพ
SAVE_CONF_THRESHOLD = 0.5            # conf ขั้นต่ำที่จะบันทึกภาพ
SAVE_COOLDOWN_SECONDS = 3            # เวลาระหว่างการบันทึกภาพซ้ำ
SAVE_DIR = "detected_waste"          # โฟลเดอร์สำหรับเก็บภาพ
```

---

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถเปิดใช้งาน virtual environment ได้

**PowerShell:**
```powershell
# ตั้งค่า execution policy
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# แล้วลองเปิดใช้งานอีกครั้ง
& .\.venv\Scripts\Activate.ps1
```

**CMD:**
```cmd
.venv\Scripts\activate.bat
```

---

### ปัญหา: pip install ล้มเหลว

```bash
# ลองอัปเดต pip ก่อน
python -m pip install --upgrade pip setuptools wheel

# หรือติดตั้งทีละ package
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
pip install ultralytics
pip install gradio
pip install opencv-python
```

---

### ปัญหา: โมเดลไม่พบ (FileNotFoundError)

ตรวจสอบว่าไฟล์โมเดลอยู่ที่ `artifacts/models/waste-sorter-best.pt`:

```bash
# ตรวจสอบว่าไฟล์มีอยู่จริง
dir artifacts\models\waste-sorter-best.pt

# ถ้าไม่มี ให้เทรนโมเดลใหม่
python train.py
```

หรือแก้ไข path ใน `app.py`:

```python
MODEL_PATH = 'artifacts/models/waste-sorter-best.pt'  # แก้ไข path ตามที่คุณมี
```

---

### ปัญหา: CUDA ไม่ทำงาน (ใช้ CPU แทน)

ตรวจสอบว่า:
1. ติดตั้ง CUDA toolkit แล้ว
2. ติดตั้ง PyTorch ที่รองรับ CUDA แล้ว
3. GPU driver อัปเดตแล้ว

```bash
# ตรวจสอบ CUDA
python -c "import torch; print(torch.cuda.is_available())"

# ถ้าเป็น False ให้ติดตั้ง PyTorch ที่รองรับ CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
```

---

### ปัญหา: Webcam ไม่ทำงาน

1. ตรวจสอบว่า webcam เปิดใช้งานแล้ว
2. ตรวจสอบว่าไม่มีโปรแกรมอื่นใช้ webcam อยู่
3. ลองรีสตาร์ทเบราว์เซอร์
4. ลองใช้เบราว์เซอร์อื่น (Chrome, Edge, Firefox)

---

### ปัญหา: เสียงไม่พูด

ตรวจสอบว่า:
1. ระบบเสียงเปิดใช้งานแล้ว
2. Volume ไม่ถูกปิด
3. ตรวจสอบ log ใน terminal ว่ามีข้อผิดพลาดหรือไม่

---

## 📚 ไฟล์สำคัญ

- `app.py` - แอปพลิเคชันหลัก (Gradio Interface)
- `voice_guidance.py` - ระบบคำแนะนำเสียง
- `train.py` - สคริปต์สำหรับเทรนโมเดล
- `evaluate.py` - สคริปต์สำหรับประเมินโมเดล
- `requirements.txt` - รายการ packages ที่ต้องติดตั้ง
- `dvc.yaml` - การตั้งค่า DVC pipeline
- `params.yaml` - พารามิเตอร์สำหรับเทรนและประเมิน

---

## 📦 การ Zip ไฟล์เพื่อย้ายไปเครื่องอื่น

หากต้องการ zip ไฟล์เพื่อย้ายไปติดตั้งที่เครื่องอื่น ให้ทำตามขั้นตอนต่อไปนี้:

### ✅ ไฟล์ที่ต้อง Zip (รวม)

#### 1. โค้ดหลัก (Code Files)
- `app.py` - แอปพลิเคชันหลัก
- `voice_guidance.py` - ระบบคำแนะนำเสียง
- `train.py` - สคริปต์เทรนโมเดล
- `evaluate.py` - สคริปต์ประเมินโมเดล
- `promote_best.py` - สคริปต์ promote โมเดล
- `test_images.py` - สคริปต์ทดสอบ (ถ้ามี)

#### 2. ไฟล์ Configuration
- `requirements.txt` - รายการ packages
- `dvc.yaml` - การตั้งค่า DVC pipeline
- `params.yaml` - พารามิเตอร์การเทรนและประเมิน
- `waste-detection.dvc` - ไฟล์ DVC สำหรับ dataset
- `.dvc/config` - การตั้งค่า DVC (ถ้ามี)
- `.gitignore` - ไฟล์ที่ git ignore

#### 3. เอกสาร
- `INSTALLATION.md` - คู่มือการติดตั้ง (ไฟล์นี้)
- `README_PIPELINE.md` - คู่มือการใช้ DVC pipeline
- `run_pipeline.bat` - Batch script สำหรับรัน pipeline

#### 4. โมเดลที่เทรนแล้ว (สำคัญ!)
- `artifacts/models/waste-sorter-best.pt` - โมเดลที่เทรนแล้ว (ไฟล์ใหญ่มาก!)

#### 5. Dataset (ถ้าต้องการ)
- `waste-detection/` - โฟลเดอร์ dataset ทั้งหมด
  - `data.yaml` - ไฟล์ config ของ dataset
  - `train/`, `valid/`, `test/` - โฟลเดอร์ข้อมูล

---

### ❌ ไฟล์ที่ไม่ต้อง Zip (ไม่รวม)

#### 1. Virtual Environment
- `.venv/` หรือ `venv/` - ไม่ต้อง zip (ให้สร้างใหม่ที่เครื่องปลายทาง)

#### 2. Cache และ Temporary Files
- `__pycache__/` - Python cache
- `.dvc/cache/` - DVC cache
- `.dvc/tmp/` - DVC temporary files
- `.gradio/` - Gradio cache

#### 3. ผลลัพธ์ที่สร้างขึ้น
- `runs/` - ผลลัพธ์การเทรน (ถ้าไม่จำเป็น)
- `detected_waste/` - ภาพที่ตรวจจับได้ (ถ้าไม่จำเป็น)
- `artifacts/eval/` - ผลการประเมิน (ถ้าไม่จำเป็น)

#### 4. Pre-trained Models อื่นๆ
- `yolo11n.pt`, `yolo11n-seg.pt`, `yolo12m.pt` - โมเดล pre-trained (Ultralytics จะดาวน์โหลดอัตโนมัติ)

#### 5. ไฟล์อื่นๆ
- `*.png`, `*.jpg` - ภาพผลลัพธ์ (ถ้าไม่จำเป็น)

---

### 📋 ขั้นตอนการ Zip

#### วิธีที่ 1: ใช้ PowerShell (แนะนำ)

```powershell
# สร้างโฟลเดอร์สำหรับ zip
New-Item -ItemType Directory -Force -Path "AI-Waste-Sorter-Package"

# คัดลอกไฟล์ที่จำเป็น
Copy-Item -Path "*.py" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "*.txt" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "*.yaml" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "*.dvc" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "*.md" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "*.bat" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path ".gitignore" -Destination "AI-Waste-Sorter-Package\" -Recurse
Copy-Item -Path "artifacts\models\waste-sorter-best.pt" -Destination "AI-Waste-Sorter-Package\artifacts\models\" -Recurse -Force
Copy-Item -Path "waste-detection" -Destination "AI-Waste-Sorter-Package\" -Recurse -Exclude "*.cache"
Copy-Item -Path ".dvc\config" -Destination "AI-Waste-Sorter-Package\.dvc\" -Recurse -Force -ErrorAction SilentlyContinue

# Zip ไฟล์
Compress-Archive -Path "AI-Waste-Sorter-Package\*" -DestinationPath "AI-Waste-Sorter.zip" -Force

# ลบโฟลเดอร์ชั่วคราว
Remove-Item -Path "AI-Waste-Sorter-Package" -Recurse -Force
```

#### วิธีที่ 2: ใช้ Windows Explorer (ง่ายที่สุด)

1. สร้างโฟลเดอร์ใหม่ชื่อ `AI-Waste-Sorter-Package`
2. คัดลอกไฟล์ต่อไปนี้เข้าไป:
   - ไฟล์ `.py` ทั้งหมด
   - `requirements.txt`
   - `dvc.yaml`, `params.yaml`
   - `waste-detection.dvc`
   - `*.md` (เอกสาร)
   - `run_pipeline.bat`
   - `.gitignore`
   - `artifacts/models/waste-sorter-best.pt` (สร้างโฟลเดอร์ `artifacts/models/` ก่อน)
   - `waste-detection/` (ทั้งโฟลเดอร์)
   - `.dvc/config` (ถ้ามี)
3. คลิกขวาที่โฟลเดอร์ `AI-Waste-Sorter-Package` → Send to → Compressed (zipped) folder

---

### 📥 วิธีติดตั้งจากไฟล์ Zip ที่เครื่องปลายทาง

1. **Extract ไฟล์ zip** ไปยังโฟลเดอร์ที่ต้องการ
2. **เปิด terminal** ในโฟลเดอร์ที่ extract แล้ว
3. **ทำตามขั้นตอนการติดตั้ง** ตั้งแต่ขั้นตอนที่ 2 (สร้าง Virtual Environment) เป็นต้นไป
   - ไม่ต้อง clone repository อีก
   - ไม่ต้องดาวน์โหลดโมเดล (ถ้า zip มาแล้ว)
   - ไม่ต้องดาวน์โหลด dataset (ถ้า zip มาแล้ว)

---

### 💡 คำแนะนำ

#### สำหรับการใช้งานทั่วไป (ไม่เทรนโมเดล):
- Zip เฉพาะ: โค้ด + โมเดล + เอกสาร
- ไม่ต้อง zip: Dataset, runs/, detected_waste/

#### สำหรับการพัฒนา/เทรนโมเดล:
- Zip ทั้งหมด: โค้ด + โมเดล + Dataset + เอกสาร
- อาจจะ zip: runs/ (ถ้าต้องการดูผลการเทรน)

#### ขนาดไฟล์โดยประมาณ:
- โค้ด + เอกสาร: ~100 KB
- โมเดล (`waste-sorter-best.pt`): ~50-200 MB (ขึ้นอยู่กับโมเดล)
- Dataset (`waste-detection/`): ~100-500 MB (ขึ้นอยู่กับขนาด)
- **รวมทั้งหมด: ~150-700 MB**

---

## 🔄 อัปเดตระบบ

```bash
# อัปเดต packages ทั้งหมด
pip install --upgrade -r requirements.txt

# อัปเดตเฉพาะ package ที่ระบุ
pip install --upgrade <package-name>
```

---

## 📝 หมายเหตุ

- Virtual environment จะต้องเปิดใช้งานทุกครั้งที่เปิด terminal ใหม่
- ระบบจะบันทึกภาพที่ตรวจจับได้ในโฟลเดอร์ `detected_waste/` (ถ้าเปิดใช้งาน)
- โมเดลที่เทรนแล้วจะถูกเก็บใน `artifacts/models/`
- Dataset จะถูกเก็บใน `waste-detection/`

---

**สร้างโดย:** พงศภัค, กฤติน, ภูริชทัต  
**อัปเดตล่าสุด:** 2025

